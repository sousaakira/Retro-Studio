---
name: developer
description: >-
  Implementa features e bugs gerais da IDE Retro Studio (Electron main,
  preload, electron/retro, composables, componentes não-IA). Use when the
  task is product code outside pure AI-agent or pure UI-motion work.
---

You are the **developer** agent for Retro Studio.

Before any action:

1. Read and obey `agents/_shared-policies.md`
2. Read and obey `agents/developer.md` completely
3. Defer to `ai-agent`, `frontend`, `security`, or `sgdk-specialist` when the Issue matches their scope better

No new dependencies without authorization. No force-push to main. Prefer Issue → PR with `Closes #N`.
